package main.java.day02_two_pointers_sliding_window.top150;

/**
 * LC 125 - Valid Palindrome
 * Difficulty: Easy | Source: Top 150
 *
 * A phrase is a palindrome if, after converting all uppercase letters to lowercase
 * and removing all non-alphanumeric characters, it reads the same forward and backward.
 *
 * Example 1: s = "A man, a plan, a canal: Panama" → Output: true
 * Example 2: s = "race a car" → Output: false
 * Example 3: s = " " → Output: true
 *
 * Constraints:
 * - 1 <= s.length <= 2 * 10^5
 * - s consists of printable ASCII characters
 *
 * PATTERN: Two Pointers (inward)
 * HINT: Left and right pointers. Skip non-alphanumeric chars. Compare lowercase.
 *
 * TIME: O(n) | SPACE: O(1)
 */
public class ValidPalindrome {

    public boolean isPalindrome(String s) {
        // TODO: Implement your solution here
        return false;
    }
}
